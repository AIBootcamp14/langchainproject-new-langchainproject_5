# 06-2. 다중 요청 처리 상세

## 📋 문서 정보
- **최초 작성일**: 2025-11-04
- **최근 업데이트**: 2025-11-04
- **시스템명**: 다중 요청 처리 시스템
- **구현 파일**: `src/agent/nodes.py`
- **우선순위**: ⭐⭐ (중요)
- **작성자**: 최현화[팀장]
- **상위 문서**: [06_AI_Agent_시스템.md](./06_AI_Agent_시스템.md)

---

## 📌 개요

다중 요청 처리 시스템은 **"논문 찾아서 요약해줘"와 같은 복합 요청을 자동으로 감지하여 여러 도구를 순차적으로 실행하는 시스템**입니다. 2025-11-04에 기본 구현이 완료되어 키워드 기반 패턴 매칭으로 다중 도구 파이프라인을 설정할 수 있게 되었습니다.

---

## 🔴 개선 전 문제점

### 문제 상황

**현상**:
- 복합 요청 시 첫 번째 도구만 실행
- 두 번째 작업은 무시됨

**예시**:
```
사용자: "Transformer 논문 찾아서 요약해줘"
   ↓
Router: "search_paper" 선택
   ↓
Agent: 논문 검색만 실행 ❌
   ↓
결과: "Attention is All You Need 논문을 찾았습니다."
(요약은 실행 안 됨)
```

**문제점**:
- 사용자가 두 번 질문해야 함
- "논문 찾아줘" → "그거 요약해줘" (2단계)
- 사용자 경험 저하

---

## ✅ 개선 후 구조

### 다중 요청 감지 로직

**구현 위치**: `src/agent/nodes.py:48-96`

```python
def router_node(state: AgentState, exp_manager=None):
    """
    라우터 노드: 질문을 분석하여 적절한 도구 선택
    + 다중 요청 감지
    """
    question = state["question"]

    # -------------- 다중 요청 감지 (키워드 기반) -------------- #
    multi_request_patterns = {
        ("찾", "요약"): ["search_paper", "summarize"],
        ("검색", "요약"): ["search_paper", "summarize"],
        ("찾", "정리"): ["search_paper", "summarize", "general"],
        ("논문", "요약"): ["search_paper", "summarize"],
        ("검색", "설명"): ["search_paper", "general"]
    }

    # 패턴 확인
    for keywords, tools in multi_request_patterns.items():
        if all(kw in question for kw in keywords):
            if exp_manager:
                exp_manager.logger.write(f"다중 요청 감지: {keywords} → {tools}")

            # tool_pipeline 설정 (순차 실행 도구 목록)
            state["tool_pipeline"] = tools
            state["tool_choice"] = tools[0]  # 첫 번째 도구부터 시작

            return state

    # -------------- 단일 요청 처리 (기존 로직) -------------- #
    # ... (LLM 라우팅 로직)

    state["tool_choice"] = tool_choice
    state["tool_pipeline"] = [tool_choice]  # 단일 도구도 파이프라인으로 관리

    return state
```

---

## 🔄 동작 흐름

### 다중 요청 처리 예시

```
사용자: "Transformer 논문 찾아서 요약해줘"
   ↓
1단계: Router 노드 - 키워드 감지
   - "찾" ✅
   - "요약" ✅
   - 패턴 매칭: ("찾", "요약") → ["search_paper", "summarize"]
   ↓
2단계: tool_pipeline 설정
   state["tool_pipeline"] = ["search_paper", "summarize"]
   state["tool_choice"] = "search_paper"  # 첫 번째 도구
   ↓
3단계: search_paper 도구 실행
   - 논문 검색
   - 결과: "Attention is All You Need 논문"
   ↓
4단계: summarize 도구 실행 (향후 graph 수정 시)
   - 검색된 논문 요약
   - 결과: "논문 요약 내용..."
   ↓
최종 답변: "Attention is All You Need 논문을 찾았고, 요약 내용은..."
```

---

## 🎯 주요 기능

### 1. 키워드 기반 패턴 매칭

**지원하는 패턴**:

| 키워드 조합 | 실행 도구 파이프라인 | 설명 |
|------------|---------------------|------|
| ("찾", "요약") | ["search_paper", "summarize"] | 논문 검색 후 요약 |
| ("검색", "요약") | ["search_paper", "summarize"] | 논문 검색 후 요약 |
| ("찾", "정리") | ["search_paper", "summarize", "general"] | 검색 → 요약 → 일반 답변 |
| ("논문", "요약") | ["search_paper", "summarize"] | 논문 검색 후 요약 |
| ("검색", "설명") | ["search_paper", "general"] | 검색 후 일반 답변 |

**패턴 추가 방법**:
```python
multi_request_patterns = {
    # ... 기존 패턴
    ("용어", "예시"): ["glossary", "general"],  # 용어 정의 → 예시 설명
    ("논문", "비교"): ["search_paper", "general"]  # 논문 검색 → 비교 분석
}
```

### 2. tool_pipeline 설정

**AgentState에 추가된 필드**:
```python
class AgentState(TypedDict):
    # ... 기존 필드
    tool_pipeline: List[str]  # 순차 실행 도구 목록
```

**예시**:
```python
state = {
    "question": "Transformer 논문 찾아서 요약해줘",
    "difficulty": "easy",
    "tool_pipeline": ["search_paper", "summarize"],
    "tool_choice": "search_paper"  # 첫 번째 도구
}
```

### 3. 로깅

**다중 요청 감지 시 로그**:
```
라우터 노드 실행: Transformer 논문 찾아서 요약해줘
다중 요청 감지: ('찾', '요약') → ['search_paper', 'summarize']
```

---

## 📊 현재 구현 상태

### ✅ 완료된 기능

1. **키워드 기반 패턴 매칭**: 5가지 패턴 지원
2. **tool_pipeline 설정**: AgentState에 도구 목록 저장
3. **첫 번째 도구 실행**: tool_choice 자동 설정
4. **로깅**: 다중 요청 감지 기록

### 🟡 부분 구현

**순차 실행 메커니즘**:
- 현재: 첫 번째 도구만 실행
- 필요: graph 수정하여 tool_pipeline 순차 실행

**향후 구현 예시**:
```python
# src/agent/graph.py (향후 개선)
def execute_pipeline(state: AgentState, exp_manager=None):
    """tool_pipeline 순차 실행"""
    pipeline = state.get("tool_pipeline", [])
    results = []

    for tool_name in pipeline:
        # 도구 실행
        result = execute_tool(state, tool_name, exp_manager)
        results.append(result)

        # 다음 도구에 결과 전달
        state["previous_result"] = result

    # 최종 결과 결합
    state["final_answer"] = combine_results(results)
    return state
```

---

## 🎯 사용 예시

### 예시 1: 논문 검색 + 요약

**질문**:
```
"BERT 논문 찾아서 요약해줘"
```

**처리 흐름**:
```
1. 키워드 감지: ("찾", "요약")
2. tool_pipeline: ["search_paper", "summarize"]
3. search_paper 실행 → "BERT 논문 발견"
4. (향후) summarize 실행 → "BERT 논문 요약"
5. 최종 답변: "BERT 논문을 찾았고, 요약은..."
```

### 예시 2: 논문 검색 + 정리

**질문**:
```
"GPT 논문 찾아서 정리해줘"
```

**처리 흐름**:
```
1. 키워드 감지: ("찾", "정리")
2. tool_pipeline: ["search_paper", "summarize", "general"]
3. search_paper 실행 → "GPT 논문 발견"
4. (향후) summarize 실행 → "GPT 논문 요약"
5. (향후) general 실행 → "요약 내용을 쉽게 설명"
6. 최종 답변: "GPT 논문을 찾아서 정리했습니다..."
```

### 예시 3: 용어 검색 + 예시

**질문 (향후 패턴 추가 시)**:
```
"Attention이 뭔지 찾아서 예시 들어줘"
```

**처리 흐름**:
```
1. 키워드 감지: ("찾", "예시")
2. tool_pipeline: ["glossary", "general"]
3. glossary 실행 → "Attention 정의"
4. (향후) general 실행 → "실생활 예시 설명"
5. 최종 답변: "Attention은... 예를 들면..."
```

---

## ⚠️ 제약사항

### 1. 키워드 기반 한계

**문제**:
- 정확한 키워드가 없으면 감지 불가
- "논문을 찾은 후 요약" → 감지 안 됨 ("찾아서" 필요)

**해결 방안 (향후)**:
- LLM 기반 의도 분석
- "논문 검색 + 요약 요청" 자동 파악

### 2. 순차 실행 미구현

**현재 상태**:
- tool_pipeline 설정만 완료
- 첫 번째 도구만 실행

**향후 개선**:
- graph 수정하여 순차 실행 구현
- 각 도구 결과를 다음 도구에 전달

### 3. 도구 간 데이터 전달

**문제**:
- search_paper 결과를 summarize에 어떻게 전달?
- state["previous_result"] 사용?

**해결 방안 (향후)**:
```python
# 도구 간 데이터 전달 예시
state["search_result"] = {
    "paper_id": "12345",
    "title": "Attention is All You Need"
}

# summarize 도구에서 참조
paper_id = state.get("search_result", {}).get("paper_id")
```

---

## 📋 향후 개선 계획

### Phase 1: LLM 기반 의도 분석 (우선순위: 높음)

**현재 (키워드 기반)**:
```python
if all(kw in question for kw in keywords):
    # 패턴 매칭
```

**향후 (LLM 기반)**:
```python
# LLM에게 의도 분석 요청
intent_prompt = """
질문: {question}

이 질문이 여러 작업을 요구하는지 분석하세요:
- 단일 작업: 하나의 도구만 필요
- 다중 작업: 여러 도구를 순차적으로 실행 필요

필요한 도구 목록을 순서대로 반환하세요.
"""

tools = llm.invoke(intent_prompt).content  # ["search_paper", "summarize"]
```

### Phase 2: 순차 실행 메커니즘 (우선순위: 높음)

**graph 수정**:
```python
# src/agent/graph.py
def should_continue_pipeline(state: AgentState):
    """tool_pipeline에 남은 도구가 있는지 확인"""
    pipeline = state.get("tool_pipeline", [])
    current_index = state.get("pipeline_index", 0)

    return current_index < len(pipeline)

# 엣지 추가
workflow.add_conditional_edges(
    "tool_executor",
    should_continue_pipeline,
    {
        True: "next_tool",
        False: END
    }
)
```

### Phase 3: 도구 체이닝 최적화 (우선순위: 중간)

**스마트 파이프라인**:
- 도구 실행 결과에 따라 다음 도구 동적 변경
- 예: search_paper 실패 시 web_search로 자동 전환

---

## 🔗 관련 문서

- **[06_AI_Agent_시스템.md](./06_AI_Agent_시스템.md)** - 상위 문서
- **[09_도구_시스템.md](./09_도구_시스템.md)** - 도구 시스템 전체
- **[09-1_도구_자동전환_기능.md](./09-1_도구_자동전환_기능.md)** - Fallback Chain
- **[docs/issues/06_Session_010_실험_분석_기반_시스템_개선.md](../issues/06_Session_010_실험_분석_기반_시스템_개선.md)** - 개선 이슈

---

## 📝 요약

### 핵심 개선사항

1. ✅ **다중 요청 감지**: 키워드 기반 패턴 매칭 (5가지 패턴)
2. ✅ **tool_pipeline 설정**: 순차 실행 도구 목록 저장
3. ✅ **첫 번째 도구 실행**: tool_choice 자동 설정
4. ✅ **로깅**: 다중 요청 감지 기록
5. 🟡 **순차 실행**: 기본 구현 완료 (graph 수정은 향후 개선)

### 현재 지원 패턴

```python
multi_request_patterns = {
    ("찾", "요약"): ["search_paper", "summarize"],
    ("검색", "요약"): ["search_paper", "summarize"],
    ("찾", "정리"): ["search_paper", "summarize", "general"],
    ("논문", "요약"): ["search_paper", "summarize"],
    ("검색", "설명"): ["search_paper", "general"]
}
```

### 사용 방법

```python
# 다중 요청이 자동으로 감지됨
사용자: "Transformer 논문 찾아서 요약해줘"

# Router 노드에서 자동 처리
state["tool_pipeline"] = ["search_paper", "summarize"]
state["tool_choice"] = "search_paper"

# 향후: 순차 실행
# search_paper → summarize → 최종 답변
```

### 주의사항

1. **키워드 정확히 입력**: "찾아서", "요약해줘" (키워드 필수)
2. **순차 실행 미구현**: 현재는 첫 번째 도구만 실행
3. **향후 개선 예정**: LLM 기반 의도 분석, graph 수정
